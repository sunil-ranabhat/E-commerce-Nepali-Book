pipeline {
    agent any

    stages {
        stage('Clone Info') {
            steps {
                echo 'Code pulled from GitHub successfully 🚀'
            }
        }

        stage('List Files') {
            steps {
                sh 'ls -la'
            }
        }
    }
}
